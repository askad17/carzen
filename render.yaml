services:
  - type: web
    name: carzen
    env: node
    region: frankfurt
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: JWT_SECRET
        value: carzen-secret-key-2026-change-this
      - key: PORT
        value: 3000